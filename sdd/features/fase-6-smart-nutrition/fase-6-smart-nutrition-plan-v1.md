# Fase 6 — Smart Nutrition Plan v1

1. Add SDD spec and tasks.
2. Add failing integration tests.
3. Extend Prisma with `FoodItem`, `MealLog`, `NutritionTarget`, and `HealthDataImport`.
4. Add validators, service methods, controllers, and routes under `/api/nutrition`.
5. Add OpenAPI docs and frontend API helpers.
6. Validate backend/frontend tests and builds.
7. Commit, push, and promote through PR chain after Phase 5 promotion is complete.
